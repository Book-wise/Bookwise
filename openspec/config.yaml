# OpenSpec Configuration — Bookwise
# Auto-generated by sdd-init

project:
  name: Bookwise
  description: Sistema de gestión de agenda para kinesiología (Kinesilk)
  language: es
  tech_stack:
    framework: Angular 21
    build: '@angular/build:application'
    language: TypeScript 5.7+
    styles: SCSS
    state: signals + @ngrx/signals
    ui_library: PrimeNG 21
    calendar: FullCalendar 6.1
    backend: Laravel REST API (http://127.0.0.1:9999/api/v1)
    zoneless: true
    standalone: true

testing:
  runner: Vitest (via @angular/build:unit-test)
  framework: Vitest 4.x
  layers:
    - unit
  config: angular.json (test builder)
  strict_tdd: false
  command: npx ng test --no-watch
  coverage: false
  existing_tests: 124 tests across 5 suites

conventions:
  commit_style: conventional-commits
  pr_style: feature-branch-chain
  component_pattern: standalone + signals
  model_file: src/app/core/models/index.ts
  services_dir: src/app/core/services/
  stores_dir: src/app/core/stores/
  features_dir: src/app/features/

hooks:
  pre-commit: review-readability
  pre-push: review-readability
  pre-pr: review-risk, review-resilience, review-readability, review-reliability
  post-sdd-phase: judgment-day
